ESCALATE_IF_FROM = [
    # Add sender domains or addresses you want to keep tabs on
    # e.g. 'amazon.com', 'bestbuy.com', 'steampowered.com'
]

ESCALATE_IF_TOPIC = [
    # Add keywords that should trigger escalation even in ads
    # e.g. 'GPU', 'mechanical keyboard', 'internship', 'scholarship'
]

DEPRIORITIZE_IF_FROM = [
    # Senders that should be classified as ADS and never escalated
    # unless the email contains an invoice, receipt, or important document
    # e.g. 'linkedin.com', 'quora.com', 'pinterest.com'
]
